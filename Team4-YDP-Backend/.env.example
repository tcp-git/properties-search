# ไฟล์สำหรับเก็บค่า Environment Variables
# ห้ามอัพโหลดไฟล์นี้ขึ้น Git (ต้องเพิ่มใน .gitignore)

# MongoDB Connection
MONGODB_URI=mongodb://localhost:27017/your_database_name

# JWT Secret Key (ใช้สำหรับเซ็น JWT token)
# ควรใช้ค่าที่ยาวและสุ่ม เช่น สร้างจาก: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your_secret_key_here_change_this_in_production

# Python Service URL (สำหรับ Search API)
PYTHON_SERVICE_URL=http://localhost:8000/api/v1/search

# Mercil API Key (สำหรับเชื่อมต่อกับ Python Service)
MERCIL_API_KEY=your_api_key_here

# Port
PORT=3000
